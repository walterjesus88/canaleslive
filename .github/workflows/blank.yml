name: Ejecución Automática

on:
  schedule:
    - cron: "19 18 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v2

    - name: Ejecutar tus comandos
      run: |
        # Agrega aquí los comandos que deseas ejecutar

    - name: Confirmar y empujar los cambios
      run: |
        git config --global user.name "walterjesus88"
        git config --global user.email "wjesus88@gmail.com"
        git add .
        git commit -m "Actualizaciones automáticas"
        git push
